---
name: Bug report / 버그 제보
about: Something doesn't work as expected / 예상대로 동작하지 않을 때
labels: bug
---

**What happened / 무슨 일이 있었나요**

**Expected / 기대한 동작**

**Steps to reproduce / 재현 방법**
1.
2.

**Environment / 환경**
- open-alive version (`open-alive --version` or package.json):
- OS:
- Node.js (`node -v`):
- Claude Code version (`claude --version`):

**`open-alive doctor` output**
```
(paste here — remove any paths or names you consider private / 민감한 경로·이름은 지워주세요)
```

**Logs (`open-alive logs`)**
```
(paste the relevant lines — never paste tokens or .env contents / 토큰·.env 내용은 붙이지 마세요)
```
