## Summary / 요약

## Test plan / 테스트
- [ ] `pnpm build`
- [ ] `pnpm test`
- [ ] `pnpm --filter=@open-alive/ui exec tsc --noEmit`

## Checklist / 체크리스트
- [ ] No secrets, tokens, `.env`, or personal paths committed / 시크릿·토큰·개인 경로 없음
- [ ] New UI strings added to both `en.json` and `ko.json` / UI 문자열 en·ko 모두 추가
